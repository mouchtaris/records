#!/usr/bin/env bash

pg_ctl stop
rm -rfv \
  "$AIRFLOW_HOME" \
  "$AIRFLOW_LOGS" \
  "$AIRFLOW_SCHEDULER_LOGS" \
  "$PGDATA" \
  "$PIPENV_CACHE_DIR" \
  "$PIP_ROOT" \
  "$PYENV_ROOT" \
  "$WORKON_HOME" \
  .venv/ \
  $HOME/.cache/pip/ \
  $HOME/.cache/pipenv/ \
  $HOME/.cache/pypoetry/ \
  $HOME/.local/app/miniconda/envs/ \
  $HOME/.local/bin/ \
  $HOME/.local/lib/
